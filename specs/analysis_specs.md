The goal is to generate many games, and then the statistics of game generation in this project.

First, we must make sure that a game generated by generator.py can be serialized as JSON, including the solution, and the statements.

In a folder "analysis" at the root, for each number of player 4, 5, 6, write a script that stores the result of 1000
game generations as a CSV

- N
  (with shill), with for each generation,
